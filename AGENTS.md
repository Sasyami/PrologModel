# Repository Guidance

When exploring this repository, treat these directories as local datasets/artifacts and keep them out of assistant context unless the user explicitly asks to work with them:

- `data/repos/`
- `data/annotated_repos/`

Prefer source files in `src/`, `data/*.py`, and `benchmark/` first.
